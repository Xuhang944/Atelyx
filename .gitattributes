# 行尾规范：仓库统一存 LF，检出强制 LF（项目全 LF，上 GitHub 零噪音）
# 不用 Windows 默认 autocrlf 的 CRLF 检出，避免跨平台 diff 噪音
* text=auto eol=lf

# 二进制文件显式声明（防 text=auto 误判）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.icns binary
*.zip binary
